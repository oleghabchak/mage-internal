import { colors } from "./colors"

export const colorsForValues: string[] = [
  colors.palette.primary,
  colors.palette.badge,
  colors.palette.lightGreen,
  colors.palette.primaryBlue,
  colors.palette.neutral500,
]
